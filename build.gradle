allprojects {
    apply plugin: "java"
    group 'com.epam.esm'

    sourceCompatibility = 1.8

    repositories {
        mavenCentral()
    }

}

version '1.0-SNAPSHOT'

dependencies {
    testImplementation group: 'junit', name: 'junit', version: '4.12'
}
