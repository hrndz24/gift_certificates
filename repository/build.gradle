dependencies {
    implementation group: 'mysql', name: 'mysql-connector-java', version: findProperty("mysql-connector.version")
    implementation group: 'org.hibernate', name: 'hibernate-core'

    testImplementation group: 'com.h2database', name: 'h2', version: findProperty("h2.version")
}
